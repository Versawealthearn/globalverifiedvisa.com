import 'bootstrap/js/dist/collapse';
import 'bootstrap/js/dist/modal';

export const initBootstrap = () => {
  // Bootstrap's JS is automatically initialized when imported
  // This function exists to maintain a consistent initialization pattern
};